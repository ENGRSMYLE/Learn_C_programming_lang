// Day one
#include <stdio.h> // standard input and output library

int main(void)
{
    int myAge = 17;
    printf("Hello , world");
    printf("I am %d years old", myAge); // %d is a format specifier
    return 0;                           // the program will return 0 when there is no bug
                                        // if there is bug it will return 1
}